#pragma once

// Copy this file to `include/wifi_secrets.h` and fill in your credentials.
// The real `wifi_secrets.h` is gitignored.

constexpr const char *WIFI_SSID = "YOUR_WIFI_NAME";
constexpr const char *WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
